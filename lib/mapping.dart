import 'package:flutter/material.dart';

class MappingPage extends StatelessWidget {
  MappingPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Hello mappingpage."));
  }
}