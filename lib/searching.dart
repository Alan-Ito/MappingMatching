import 'package:flutter/material.dart';

class SearchingPage extends StatelessWidget {
  SearchingPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Hello searchingpage."));
  }
}